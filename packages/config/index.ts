// Shared configuration constants used by both apps/api and apps/web.
// Empty for now — will hold things like pagination defaults, plan tiers,
// and feature flags as modules are added.

export const APP_NAME = "Atelier ERP";
